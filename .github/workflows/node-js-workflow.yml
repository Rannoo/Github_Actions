name: NodeJS API 
on: push

jobs:
  node-api-docker:
    runs-on: ubuntu-latest
    services:
        mongo:
          image: mongo
          ports:
            - "27017:27017"
        app:
          image: alialaa17/node-api:latest
          ports:
            - "3001:3000"
    steps:
      #- name: Post a user
        #run: "curl -X POST http://0.0.0.0:3001/api/user 
        #-H 'Content-Type: application/json' 
        #-d '{\"username\": \"Ranya\",\"address\": \"Tunisia\"}'"
      - name: Log docker
        run: docker ps -a
      - name: Get users
        run: "curl http://localhost:3001/api/users"